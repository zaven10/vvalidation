{
  "name": "vvalidation",
  "version": "1.0.0",
  "description": "Decorator-based property validation for classes (vue3 / vuelidate 2).",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "repository": {
    "type": "git",
    "url": "https://github.com/zaven10/vvalidation.git"
  },
  "scripts": {
    "format": "prettier --write \"src/**/*.ts\" \"tests/**/*.ts\"",
    "start": "npm run build && node tests/test.js",
    "build": "npm run clean && npx tsc",
    "clean": "shx rm -rf dist"
  },
  "tags": [
    "vvalidation",
    "validator",
    "validation",
    "decorators",
    "typescript"
  ],
  "keywords": [
    "vvalidation",
    "vuelidate 2 validation",
    "vue3 validation",
    "vuelidate",
    "v validation",
    "vue validation",
    "vuelidate class validator",
    "vuelidate decorators",
    "composition api validation"
  ],
  "author": "Zaven",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^2.7.1",
    "shx": "^0.3.4",
    "typescript": "^4.9.3"
  },
  "dependencies": {
    "@vuelidate/core": "^2.0.0",
    "@vuelidate/validators": "^2.0.0",
    "vue": "^3.2.45"
  }
}
